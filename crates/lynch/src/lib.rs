mod math;
mod constants;
mod camera;
pub mod window;
pub mod vulkan;
pub mod renderer;
pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        assert_eq!(add(2, 2), 4);
    }
}
